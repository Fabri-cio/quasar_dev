<section
  x-data="{ show: false }"
  x-init="setTimeout(() => show = true, 300)"
  class="max-w-6xl mx-auto px-4 py-20 text-center"
>
  <div
    x-show="show"
    x-transition:enter="transition duration-1000"
    x-transition:enter-start="opacity-0 translate-y-20 scale-95"
    x-transition:enter-end="opacity-100 translate-y-0 scale-100"
  >
    <h2 class="text-4xl md:text-6xl font-bold leading-tight">
      Creamos páginas web y sistemas que
      <span class="text-blue-500">generan clientes</span>
    </h2>

    <p class="mt-6 text-slate-400 max-w-2xl mx-auto">
      Ayudamos a negocios a crecer con soluciones digitales modernas enfocadas
      en resultados.
    </p>

    <div class="mt-8 flex justify-center gap-4">
      <a
        href="#contacto"
        class="bg-linear-to-r from-blue-600 to-purple-600 px-6 py-3 rounded-xl"
      >
        Cotizar mi proyecto
      </a>

      <a href="#proyectos" class="border border-slate-700 px-6 py-3 rounded-xl">
        Ver trabajos
      </a>
    </div>

    <p class="text-sm text-slate-500 mt-4">Respuesta en menos de 24 horas</p>
  </div>
</section>
